import { Meta } from "@storybook/blocks"

<Meta title="Introduction" />

# Introduction

This component library is split up into three categories:

- Atoms
  - Includes all unique single components that have no dependencies on other components
- Molecules
  - Includes all unique components that depend on at least one other component
- Ecosystems
  - Includes complete examples of collections of molecules. These will not be exported
